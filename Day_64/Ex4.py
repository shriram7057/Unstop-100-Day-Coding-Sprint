def determine_winner(d, k):
    """
    Write your logic here.
    Parameters:
        d (int): Euclidean distance from the origin point
        k (int): The token by increasing either the x-coordinate or the y-coordinate by exactly k units
    Returns:
        str: "Ashish" if Ashish wins the game, "Utkarsh" if Utkarsh wins
    """
    pass

def main():
    import sys
    input = sys.stdin.read
    data = input().strip().split()
    
    d = int(data[0])  # First input is the integer d
    k = int(data[1])  # Second input is the integer k
    
    # Call user logic function and print the output
    result = determine_winner(d, k)
    print(result)

if __name__ == "__main__":
    main()def determine_winner(d, k):
    """
    Write your logic here.
    Parameters:
        d (int): Euclidean distance from the origin point
        k (int): The token by increasing either the x-coordinate or the y-coordinate by exactly k units
    Returns:
        str: "Ashish" if Ashish wins the game, "Utkarsh" if Utkarsh wins
    """
    pass

def main():
    import sys
    input = sys.stdin.read
    data = input().strip().split()
    
    d = int(data[0])  # First input is the integer d
    k = int(data[1])  # Second input is the integer k
    
    # Call user logic function and print the output
    result = determine_winner(d, k)
    print(result)

if __name__ == "__main__":
    main()